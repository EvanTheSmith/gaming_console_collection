<!DOCTYPE html>
  <head>
    <title>Gaming Console Collection</title>
    <link rel="stylesheet" href="/stylesheets/main.css" />
  </head>
  
  <body>

   <div class="wrapper">
   <% flash.keys.each do |type| %>
    <div class="flash_<%= type %>">
      <%= flash[type] %>
    </div>
  <% end %>
  
   <img src="/images/header_banner.png" class="img-responsive main-image">
   <table class="topbar">
   <tr><td><h2><a href="/">Home</a> // <a href="/users">Collectors</a> // <a href="/consoles">Game Consoles</a></h2></td>
   <td><% if logged_in? %>
   <h2 style="text-align:right"><a href="/logout">Sign Out</a></h2>
   <% else %>
   <h2 style="text-align:right"><a href="/login">Sign In</a></h2>
   <% end %></td></tr>
   </table>
   <hr />
   
   <%= yield %>

   </div>
  </body>
</html>
